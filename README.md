# brainiax
A puzzle app for Android and iOS

Packages:
- /app — Flutter UI
- /puzzle_core — Pure Dart: generators, solvers, validators, difficulty scoring
- /assets — Fonts, SFX, word lists (open-source)

## Dev
- `melos bootstrap` to link packages
- `melos analyze`, `melos test`, `melos format`
