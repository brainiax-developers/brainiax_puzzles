name: puzzle_core
publish_to: none
resolution: workspace

description: On-device deterministic puzzle generation with pluggable engines and stable API.
version: 1.0.0
repository: https://github.com/brainiax/puzzles

environment:
  sdk: ^3.9.0

# Add regular dependencies here.
dependencies:
  # No external dependencies - keeping it minimal for now

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
  mocktail: ^1.0.3