{
  "name": "Dart Dev",
  "build": { "dockerfile": "Dockerfile" },
  "features": {},
  "postCreateCommand": "dart --version && dart pub get || true",
  "customizations": {
    "vscode": {
      "extensions": [
        "Dart-Code.dart-code",
        "Dart-Code.flutter" // safe even if you don't use Flutter
      ]
    }
  }
}
